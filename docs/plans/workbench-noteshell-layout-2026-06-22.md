# ④ 2/14 · 首页合集布局 + 结果页标题收敛（2026-06-22）

> 来源：手测 19 条第 ④「添加·布局」组，用户选**先做 2/14**（明确 UI 调整）。
> 15 左侧导航+设计回顾、16 字幕直取（独立大功能）单独再做。

---

## 2 · 首页「选择合集」浮层与最近任务重叠
- **现状**：Composer 合集选择浮层 `.pp-popover`（`Composer.tsx:275`，`wsOpen` 时显示，已有
  `popRef` 点外关闭）和下方 `RecentTasks` **重叠/互相遮挡**（手测截图）。
- **根因**：浮层的 `z-index` / 定位不足，被最近任务区压住或挤位。
- **修**：定位 `.pp-popover` 的 CSS——
  - `z-index` 提到最近任务之上；`position` 锚定「选择合集」按钮（absolute 相对合集行）；
  - 确保浮层完整悬浮、不推挤页面、不和最近任务重叠。
- **涉及**：`Composer.tsx` + `.pp-popover` 对应 CSS（rg 定位样式文件）。

## 14 · 结果页标题重复 + 原视频详情/标签
- **现状**：
  - 顶栏 `vd-title`（`index.tsx:861`）= `{title}` + 类型标签
  - 视频 banner（`:1184-1212`）= `{title}` + 平台 + 「原视频 ↗」
  - → **同一标题两处重复**。
  - 标签 `TagChips`（`:393`）**已有**，在可折叠标签条（`:1158`）。
- **修（推荐，待用户确认细节）**：
  - **顶栏去掉重复标题**：`vd-title` 保留 返回 + 类型 + 操作按钮（版本▾/导出/AI工具），不再重复 `{title}`。
  - **banner 作为「原视频详情」**：标题 + 平台 + 原视频↗（已有），把标签 `TagChips` 整合到 banner 区/紧邻
    （一处看 原视频详情 + 标签），或保留现折叠标签条紧贴 banner。
- **涉及**：`NoteShell/index.tsx`（顶栏 `vd-title`、banner、标签条位置）。

## 验收
- 首页：「选择合集」浮层弹出时完整悬浮、**不和最近任务重叠/遮挡**。
- 结果页：标题只出现一处（banner），顶栏不再重复；banner 含 标题+平台+原视频↗+标签。
- `npm test`（相关）+ `npm run build` 全绿；`./dev.sh` 实跑确认。

## 红线
- 纯 UI 布局，不动数据/后端。
- 14 顶栏去标题后确认布局不塌、类型/操作按钮正常；标签整合不丢 TagChips 现有能力。
- 不动 15/16；不 git push；干净 worktree 核对；UI 改动 `./dev.sh` 实跑。
