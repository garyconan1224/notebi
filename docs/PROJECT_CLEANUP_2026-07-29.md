# NoteBi 项目清理记录（2026-07-29）

## 目标

在继续开发前，把未完成工作、历史分支、运行数据、旧计划和当前产品事实分开，建立可恢复、可验证的干净基线。

## 已处理

- 当前未提交源码保存到 `codex/wip-batch-settings-cleanup` 的 `e90785b`。
- 未合并旧分支和原 stash 建立归档标签后移除活动引用。
- 删除已合并旧分支和多余 worktree，只保留 `main`、清理分支和 WIP 分支。
- 从当前树删除 195 份历史计划、旧多产品规格、旧截图和截图型验收证据。
- 保留精简 JSON 验收报告和验收脚本。
- 旧 Windows 离线包、构建产物和缓存移到系统废纸篓。
- 增加运行日志、批次记录、WorkBuddy、Qoder 和环境模板的忽略规则。
- 重建产品决策、交接、工作流和文档索引。
- 将 `local_settings.example.py` 中疑似真实的 SiliconFlow 密钥替换为明确占位符。

## 明确保留

- `data/workspaces`、媒体、索引和设置
- `.local`
- `.venv`
- `frontend/node_modules`
- 当前模型和 Provider 配置
- 旧 Replica 数据清理及旧接口拒绝等升级安全代码
- 清理前检查点和历史 Git 提交

## 恢复点

- `checkpoint/pre-cleanup-20260729`
- `checkpoint/main-sync-a-20260729`
- `archive/complete-approved-plan-20260729`
- `archive/pre-main-sync-user-work-20260729`

系统废纸篓中的本轮目录为 `notebi-cleanup-20260729`。确认项目长期稳定后再由用户决定是否彻底清空。

## 待用户决定的安全项

- Git 历史中仍有 1 个唯一的 OpenAI 风格密钥字符串，最早由 `33c5cf3` 引入。
- 当前仓库没有 remote，当前树已经不再包含该字符串。
- 如果该值曾经真实可用，应先在对应服务商处轮换；是否重写本地 Git 历史必须由用户单独确认。

## 清理分支验证

- 前端完整测试：`53` 个测试文件、`293` 项测试通过。
- 前端生产构建：通过；保留一个既有的大包体积警告。
- 后端完整测试：`1218 passed, 2 skipped`。
- Python 编译检查：通过。
- Python 依赖一致性：`pip check` 通过。
- Git 补丁格式：`git diff --check` 通过。
- 前端 lint：未通过，当前基线有 `101` 个错误、`8` 个警告，分布于大量本轮未修改文件；作为独立技术债处理，不并入项目清理。

## 清理后仍需确认

- 视频预检仍有“音乐分析”，并包含 Suno / Udio 格式输出。它是否属于应删除的提示词生产能力，需要用户确认后再改。
- 前端 lint 基线、批次生命周期和导出多选语义属于后续短期计划候选，不在本次清理中顺手扩展。
