# 知识库 / 收藏夹 / 搜索调研 — 概述

> 完整方案见 `docs/plans/knowledge-favorites-search-research-2026-07-23.md`
> 总体执行计划见 `docs/plans/knowledge-favorites-search-master-execution-plan-2026-07-25.md`
> 状态：产品方向和总体执行计划已建立，待从 Phase 0 开始逐阶段执行

## 核心结论

- 一个“智能检索”页面，默认同时展示 AI 综合回答和可核验、可跳转的原文来源。
- 另设“精确查找”模式：只做关键词匹配和原文定位，不生成 AI 回答。
- 回答引用可定位来源卡片；来源可展开上下文、跳转并一键收藏。
- 内容库继续负责合集、标签、文件夹、同源副本和用户笔记正文版本。
- 素材加入另一个合集时生成独立副本；副本可独立修改，同时能查看其他合集中的同源版本，不自动互相覆盖。
- AI 总结已有版本机制；新增版本控制只先覆盖用户手工编辑的笔记正文。

## 复审后的关键修正

- 知识库问答已经使用 reranker；与搜索的真实差异是索引、缓存和跨合集候选合并方式。
- 当前 JSON 数据已出现相同 `item_id` 跨合集复用，SQLite 不能只用 `item_id` 关联标签和版本。
- SQLite 先作为可重建 FTS5 索引；身份和元数据稳定后再成为标签、文件夹、收藏和正文版本的唯一事实来源。
- FTS5 可用，但默认分词不能满足中文精确查找，必须验证中文分词或多粒度 n-gram 方案。
- 当前收藏记录为 0，首期先做“检索结果一键收藏”，收藏分组后置。

## 重构后三个职责

| 模块 | 职责 |
|---|---|
| 智能检索 | AI 回答、原文证据、精确查找 |
| 内容库管理 | 合集、独立副本、同源谱系、标签、正文版本 |
| 收藏夹 | 接收检索结果、快速回到重要内容 |

## 分期

1. Phase 0：补契约测试、数据基线和旧路由兼容约定。
2. Phase A：合并智能检索页面、引用来源与一键收藏；不改数据层。
3. Phase B：建立可重建的分块 FTS5 索引，上线精确查找。
4. Phase C：迁移独立内容身份、同源谱系和权威元数据。
5. Phase D：用户笔记正文版本与同源副本查看。
6. Phase E：混合检索、收藏分组、批量整理等增强项。

## 执行边界

页面合并、FTS、数据身份迁移和正文版本系统必须分阶段实施；每阶段均需独立测试、数据校验和回滚边界，不能一次性大改。
